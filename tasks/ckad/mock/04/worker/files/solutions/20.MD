```
kubectl config use-context cluster1-admin@cluster1
```
```
# View the deprecated manifest
cat /home/ubuntu/deprecated.yaml
# Note: it uses apiVersion: batch/v1beta1 which was removed in k8s 1.25
```
```
# Fix: change apiVersion from batch/v1beta1 to batch/v1
sed -i 's|apiVersion: batch/v1beta1|apiVersion: batch/v1|g' /home/ubuntu/deprecated.yaml

# Verify the fix
cat /home/ubuntu/deprecated.yaml | grep apiVersion
```
```
# Apply the fixed manifest
kubectl apply -f /home/ubuntu/deprecated.yaml

# Verify
kubectl get cronjob old-job -n deprecated-ns
```
