```
kubectl config use-context cluster1-admin@cluster1
```
```
kubectl create ns jobs-ns
```
```
# vim 5.yaml

apiVersion: batch/v1
kind: Job
metadata:
  name: batch-job
  namespace: jobs-ns
spec:
  completions: 8
  parallelism: 4
  backoffLimit: 3
  template:
    spec:
      containers:
      - name: batch-job
        image: busybox
        command: ["echo", "processing item"]
      restartPolicy: Never
```
```
kubectl apply -f 5.yaml

kubectl get job batch-job -n jobs-ns
kubectl get pods -n jobs-ns
```
