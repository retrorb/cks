```
# On the worker PC
```
```
# Build the image
podman build -t myapp:v1 /home/ubuntu/app/

# Verify it outputs "Hello CKAD"
podman run --rm myapp:v1

# Save the image as tar
podman save myapp:v1 -o /home/ubuntu/myapp.tar

# Verify
ls -lh /home/ubuntu/myapp.tar
podman images myapp:v1
```
