```
kubectl config use-context cluster1-admin@cluster1
```
```
# Check existing deployment
kubectl get deployment startup-app -n startup-ns
```
```
kubectl patch deployment startup-app -n startup-ns --type=json -p='[
  {"op":"add","path":"/spec/template/spec/containers/0/readinessProbe","value":{
    "httpGet":{"path":"/health","port":8080},
    "initialDelaySeconds":5,
    "periodSeconds":10
  }},
  {"op":"add","path":"/spec/template/spec/containers/0/startupProbe","value":{
    "httpGet":{"path":"/health","port":8080},
    "failureThreshold":30,
    "periodSeconds":10
  }}
]'
```
```
# Verify
kubectl get deployment startup-app -n startup-ns -o jsonpath='{.spec.template.spec.containers[0].readinessProbe}'
kubectl get deployment startup-app -n startup-ns -o jsonpath='{.spec.template.spec.containers[0].startupProbe}'
kubectl rollout status deployment/startup-app -n startup-ns
```
