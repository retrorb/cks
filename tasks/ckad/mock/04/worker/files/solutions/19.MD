```
kubectl config use-context cluster1-admin@cluster1
```
```
# Wait for metrics-server to be ready if needed
kubectl top nodes

# Save node metrics
kubectl top nodes > /home/ubuntu/top-nodes.txt

# Save pod metrics for all namespaces
kubectl top pods -A > /home/ubuntu/top-pods.txt

# Verify files
cat /home/ubuntu/top-nodes.txt
cat /home/ubuntu/top-pods.txt
```
