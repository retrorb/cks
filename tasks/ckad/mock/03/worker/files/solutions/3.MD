```
kubectl config use-context cluster1-admin@cluster1
```
```
kubectl get deployment update-app -n update-ns -o jsonpath='{.spec.template.spec.containers[0].image}'
```
```
kubectl set image deployment/update-app update-app=nginx:alpine -n update-ns

# Monitor the rollout
kubectl rollout status deployment/update-app -n update-ns

# Verify
kubectl get deployment update-app -n update-ns -o jsonpath='{.spec.template.spec.containers[0].image}'
```
