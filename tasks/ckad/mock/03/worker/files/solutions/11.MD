```
kubectl config use-context cluster1-admin@cluster1
```
```
# Check existing deployment
kubectl get deployment web-app -n svc-ns --show-labels
```
```
kubectl expose deployment web-app -n svc-ns \
  --name=web-svc \
  --type=ClusterIP \
  --port=80 \
  --target-port=80

# Or via yaml:
# kubectl create svc clusterip web-svc -n svc-ns --tcp=80:80 --dry-run=client -o yaml

# Verify
kubectl get svc web-svc -n svc-ns
kubectl get endpoints web-svc -n svc-ns
```
