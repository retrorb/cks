```
kubectl config use-context cluster1-admin@cluster1
```
```
# Check current state
kubectl get svc web-svc -n svc-fix-ns -o jsonpath='{.spec.selector}'
kubectl get deployment web-deploy -n svc-fix-ns --show-labels
kubectl get pods -n svc-fix-ns --show-labels
kubectl get endpoints web-svc -n svc-fix-ns
```
```
kubectl patch svc web-svc -n svc-fix-ns -p '{"spec":{"selector":{"app":"web-v2"}}}'

# Verify
kubectl get svc web-svc -n svc-fix-ns -o jsonpath='{.spec.selector}'
kubectl get endpoints web-svc -n svc-fix-ns
```
