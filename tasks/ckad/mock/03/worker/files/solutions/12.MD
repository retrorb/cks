```
kubectl config use-context cluster1-admin@cluster1
```
```
# Check existing deployment
kubectl get deployment np-app -n np-ns --show-labels
```
```
# vim 12.yaml

apiVersion: v1
kind: Service
metadata:
  name: np-svc
  namespace: np-ns
spec:
  type: NodePort
  selector:
    app: np-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30091
```
```
kubectl apply -f 12.yaml

# Verify
kubectl get svc np-svc -n np-ns
```
