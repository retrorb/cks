```
kubectl config use-context cluster1-admin@cluster1
```
```
# Check the broken deployment
kubectl get deployment broken-deploy -n broken-ns
kubectl get pods -n broken-ns
```
```
kubectl set image deployment/broken-deploy broken-deploy=nginx:alpine -n broken-ns

# Monitor
kubectl rollout status deployment/broken-deploy -n broken-ns

# Verify
kubectl get deployment broken-deploy -n broken-ns
kubectl get pods -n broken-ns
```
