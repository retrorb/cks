```
kubectl config use-context cluster1-admin@cluster1
```
```
# Check existing deployment
kubectl get deployment final-app -n final-ns --show-labels
```
```
# vim 20.yaml

apiVersion: v1
kind: Service
metadata:
  name: final-svc
  namespace: final-ns
spec:
  type: ClusterIP
  selector:
    app: final-app
  ports:
  - port: 80
    targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: final-ingress
  namespace: final-ns
spec:
  ingressClassName: nginx
  rules:
  - host: final.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: final-svc
            port:
              number: 80
```
```
kubectl apply -f 20.yaml

# Verify
kubectl get svc final-svc -n final-ns
kubectl get ingress final-ingress -n final-ns
kubectl describe ingress final-ingress -n final-ns
```
