```
kubectl config use-context cluster1-admin@cluster1
```
```
# Check the broken ingress
kubectl get ingress broken-ingress -n ingress-fix-ns -o yaml
kubectl get svc -n ingress-fix-ns
```
```
kubectl edit ingress broken-ingress -n ingress-fix-ns
# Change: name: wrong-svc  ->  name: correct-svc

# Or patch:
kubectl patch ingress broken-ingress -n ingress-fix-ns --type=json \
  -p='[{"op":"replace","path":"/spec/rules/0/http/paths/0/backend/service/name","value":"correct-svc"}]'

# Verify
kubectl get ingress broken-ingress -n ingress-fix-ns -o jsonpath='{.spec.rules[0].http.paths[0].backend.service.name}'
```
