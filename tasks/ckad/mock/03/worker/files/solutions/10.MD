```
kubectl config use-context cluster1-admin@cluster1
```
```
# Check existing setup
kubectl get deployment main-app -n canary-ns
kubectl get svc frontend-svc -n canary-ns -o jsonpath='{.spec.selector}'
```
```
# vim 10.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: canary-app
  namespace: canary-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
      track: canary
  template:
    metadata:
      labels:
        app: frontend
        track: canary
    spec:
      containers:
      - name: canary-app
        image: viktoruj/ping_pong:alpine
        ports:
        - containerPort: 8080
```
```
kubectl apply -f 10.yaml

# Verify - service routes to both stable and canary (selector: app=frontend)
kubectl get deployment -n canary-ns
kubectl get pods -n canary-ns --show-labels
```
