```
kubectl config use-context cluster1-admin@cluster1
```
```
# Check existing StatefulSet
kubectl get statefulset db-stateful -n headless-ns --show-labels
```
```
# vim 19.yaml

apiVersion: v1
kind: Service
metadata:
  name: db-headless
  namespace: headless-ns
spec:
  clusterIP: None
  selector:
    app: db-stateful
  ports:
  - port: 5432
    targetPort: 5432
```
```
kubectl apply -f 19.yaml

# Verify
kubectl get svc db-headless -n headless-ns
kubectl describe svc db-headless -n headless-ns
```
