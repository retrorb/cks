# Allowed resources

## **Kubernetes Documentation:**

<https://kubernetes.io/docs/> and their subdomains

<https://kubernetes.io/blog/> and their subdomains

<https://helm.sh/> and their subdomains

This includes all available language translations of these pages (e.g. <https://kubernetes.io/zh/docs/>)

- run ``time_left`` on work pc to **check time**
- run ``check_result`` on work pc to **check result**

## Questions

|        **1**        | **Install an application with Helm**                                                                                                                                                                                                                                      |
| :-----------------: | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
|     Task weight     | 4%                                                                                                                                                                                                                                                                        |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                           |
| Acceptance criteria | - Add Helm repository `bitnami` using URL `https://charts.bitnami.com/bitnami`<br/>- Create namespace `helm-shop-ns`<br/>- Install chart `bitnami/nginx` as release name `shop-web` in namespace `helm-shop-ns`<br/>- Set `replicaCount=3` and `service.type=ClusterIP` |
---
|        **2**        | **Upgrade an existing Helm release**                                                                                                                                                              |
| :-----------------: | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
|     Task weight     | 4%                                                                                                                                                                                                |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                   |
| Acceptance criteria | Helm release `data-api` already exists in namespace `helm-api-ns` with `replicaCount=1`.<br/>Upgrade the release to set `replicaCount=4`. Ensure the release status is `deployed` after upgrade. |
---
|        **3**        | **Rollback a Helm release**                                                                                                                                                                                                                                                                               |
| :-----------------: | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                                        |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                           |
| Acceptance criteria | Helm release `report-app` in namespace `helm-report-ns` was upgraded with a bad image tag and pods are failing.<br/>- Roll back the release to revision `1`<br/>- After rollback the deployment should have `2` replicas and pods should be running<br/>- Release status must be `deployed` |
---
|        **4**        | **Install a Helm release using a values file**                                                                                                                                                                                                                                |
| :-----------------: | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                             |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                |
| Acceptance criteria | A values file exists at `/home/ubuntu/helm-values/metrics.yaml`.<br/>- Create namespace `helm-metrics-ns`<br/>- Install chart `bitnami/nginx` as release name `metrics-web` in namespace `helm-metrics-ns` using this values file<br/>- At least `2` pods must be running and service type must be `ClusterIP` |
---
|        **5**        | **Delete a broken Helm release and install a fresh one**                                                                                                                                                                                                                           |
| :-----------------: | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 3%                                                                                                                                                                                                                                                                                 |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                    |
| Acceptance criteria | Helm release `broken-release` exists in namespace `helm-broken-ns` but its pods are failing due to a bad image tag.<br/>- Uninstall release `broken-release`<br/>- Install chart `bitnami/nginx` as release name `fixed-release` in namespace `helm-broken-ns` with `replicaCount=2` |
---
|        **6**        | **Deploy with Kustomize using an image overlay**                                                                                                                                                                                                                                               |
| :-----------------: | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                                               |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                                  |
| Acceptance criteria | Base kustomize files exist at `/home/ubuntu/kust2/base/` (Deployment `kust2-app` using `nginx:alpine`).<br/>Create a staging overlay at `/home/ubuntu/kust2/overlays/staging/kustomization.yaml` that:<br/>- Sets namespace to `kust2-ns`<br/>- Changes the image from `nginx:alpine` to `nginx:1.25` using the `images` transformer<br/>Apply the overlay. Namespace `kust2-ns` already exists. |
---
|        **7**        | **Generate a ConfigMap with Kustomize**                                                                                                                                                                                                                                                                                                      |
| :-----------------: | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                                                                           |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                                                              |
| Acceptance criteria | Create a kustomization file at `/home/ubuntu/kust3/kustomization.yaml` that:<br/>- Sets namespace to `kgen-ns`<br/>- Uses `configMapGenerator` to create ConfigMap named `app-config` with literals: `APP_MODE=production`, `LOG_LEVEL=info`<br/>- Use `generatorOptions: disableNameSuffixHash: true` to preserve the exact name<br/>Apply the kustomization. Namespace `kgen-ns` already exists. |
---
|        **8**        | **Apply namePrefix and commonLabels with Kustomize**                                                                                                                                                                                                                                                                                                                                                         |
| :-----------------: | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                                                                                                                                           |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                                                                                                                              |
| Acceptance criteria | Base kustomize files exist at `/home/ubuntu/kust4/base/` (Deployment `kust4-app`, Service `kust4-svc`).<br/>Create a production overlay at `/home/ubuntu/kust4/overlays/prod/kustomization.yaml` that:<br/>- Sets namespace to `kprod-ns`<br/>- Adds `namePrefix: prod-` to all resources<br/>- Adds `commonLabels: env: production` to all resources<br/>Apply the overlay. Result: Deployment `prod-kust4-app` and Service `prod-kust4-svc` in `kprod-ns` with label `env=production`. |
---
|        **9**        | **Add resource limits with a Kustomize strategic merge patch**                                                                                                                                                                                                                                                                                                         |
| :-----------------: | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                                                                                                       |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                                                                                          |
| Acceptance criteria | Base kustomize files exist at `/home/ubuntu/kust5/base/` (Deployment `cache-app` without resource limits).<br/>Create a production overlay at `/home/ubuntu/kust5/overlays/prod/` that:<br/>- Sets namespace to `kpatch-ns`<br/>- Uses a strategic merge patch to add resource limits: `cpu: 200m`, `memory: 256Mi` to the `cache-app` container<br/>Apply the overlay. Namespace `kpatch-ns` already exists. |
---
|       **10**        | **Blue/Green deployment — switch traffic to v2**                                                                                                                                                                                                                                                                                                          |
| :-----------------: | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                                                                                        |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                                                                           |
| Acceptance criteria | In namespace `bg2-ns`: Deployment `v1-deploy` (image=`nginx:alpine`, label `version=v1`), Service `v1-svc` (selector: `version=v1`), and Ingress `bg-ingress` (routing to `v1-svc`) already exist.<br/>- Create Deployment `v2-deploy` (image=`viktoruj/ping_pong:alpine`, label `version=v2`, replicas=`2`)<br/>- Create Service `v2-svc` (selector: `version=v2`, port=`80`)<br/>- Update Ingress `bg-ingress` backend to point to `v2-svc` |
---
|       **11**        | **Canary deployment using Ingress annotations**                                                                                                                                                                                                                                                                                                                                                                     |
| :-----------------: | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                                                                                                                                                   |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                                                                                                                                      |
| Acceptance criteria | In namespace `canary2-ns`: Deployment `stable-app2` (`4` replicas, `nginx:alpine`), Service `stable-svc2`, and Ingress `stable-ingress2` (host=`app2.example.com`) already exist.<br/>- Create Deployment `canary-app2` (`1` replica, image=`viktoruj/ping_pong:alpine`, labels: `app=webapp2, track=canary`)<br/>- Create Service `canary-svc2` (selector: `app=webapp2, track=canary`, port=`80`)<br/>- Create Ingress `canary-ingress2` with annotations: `nginx.ingress.kubernetes.io/canary: "true"`, `nginx.ingress.kubernetes.io/canary-weight: "20"`, host=`app2.example.com`, backend=`canary-svc2:80` |
---
|       **12**        | **Change deployment strategy to Recreate**                                                                                                                                                                                                                                      |
| :-----------------: | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 3%                                                                                                                                                                                                                                                                               |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                  |
| Acceptance criteria | Deployment `batch-proc` in namespace `recreate-ns` currently uses `RollingUpdate` strategy and has image `nginx:1.24`.<br/>- Change the deployment strategy to `Recreate`<br/>- Update the container image to `nginx:alpine`<br/>- Ensure at least `1` replica is ready after the change |
---
|       **13**        | **NetworkPolicy: Default deny all ingress**                                                                                                                                                                                                       |
| :-----------------: | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                   |
| Acceptance criteria | In namespace `deny-all-ns` there is a pod `target-pod`. In namespace `ext-ns` there is a pod `tester-pod`.<br/>Create a NetworkPolicy in namespace `deny-all-ns` that:<br/>- Denies ALL ingress traffic to ALL pods (empty `podSelector: {}`)<br/>- After applying, `tester-pod` in `ext-ns` must NOT be able to reach `target-pod` on port `8080` |
---
|       **14**        | **NetworkPolicy: Allow only api pods to reach db**                                                                                                                                                                                                                                                                                |
| :-----------------: | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                                                                |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                                                   |
| Acceptance criteria | In namespace `tier-ns` there are pods: `api-pod` (label: `role=api`), `worker-pod` (label: `role=worker`), `db-pod` (label: `role=db`, serving on port `8080`).<br/>Create a NetworkPolicy that:<br/>- Allows ingress to `db-pod` only from pods with label `role=api` on port `8080`<br/>- Denies all other ingress to `db-pod`<br/>After applying: `api-pod` can reach `db-pod`, `worker-pod` cannot |
---
|       **15**        | **NetworkPolicy: Restrict egress to HTTPS and DNS only**                                                                                                                                                                                                        |
| :-----------------: | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                               |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                  |
| Acceptance criteria | In namespace `egress-ns` there is a pod `worker-egress` (label: `app=worker`).<br/>Create a NetworkPolicy in `egress-ns` that:<br/>- Applies `policyTypes: [Egress]`<br/>- Allows outbound TCP on port `443` (HTTPS)<br/>- Allows outbound UDP and TCP on port `53` (DNS)<br/>- Blocks all other egress |
---
|       **16**        | **NetworkPolicy: Cross-namespace with combined selector**                                                                                                                                                                                                                                                                                   |
| :-----------------: | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                                                                          |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                                                             |
| Acceptance criteria | Namespace `secure-ns` has Deployment `vault-app` (label: `app=vault`, serving port `8080`). Namespace `admin-ns` (label: `team=admin`) has pod `admin-pod` (label: `role=operator`). Namespace `other16-ns` has pod `other-pod`.<br/>Create a NetworkPolicy in `secure-ns` that:<br/>- Allows ingress to `vault-app` pods ONLY from pods with label `role=operator` in namespaces with label `team=admin`<br/>- Use a combined `namespaceSelector` AND `podSelector` (both in the same `from` entry) |
---
|       **17**        | **NetworkPolicy: Allow ingress from multiple pod selectors (OR logic)**                                                                                                                                                                                                                                                                                 |
| :-----------------: | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                                                                                      |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                                                                         |
| Acceptance criteria | In namespace `multi-src-ns`: pod `api-pod17` (label: `role=api`, port `8080`), pod `frontend-pod17` (label: `role=frontend`), pod `monitor-pod17` (label: `role=monitor`), pod `blocked-pod17` (label: `role=blocked`).<br/>Create a NetworkPolicy allowing ingress to `api-pod17` on port `8080` from pods with label `role=frontend` OR `role=monitor`.<br/>After applying: both `frontend-pod17` and `monitor-pod17` can reach `api-pod17`, but `blocked-pod17` cannot. |
---
|       **18**        | **NetworkPolicy: Restrict egress from client to backend only**                                                                                                                                                                                                                                                                          |
| :-----------------: | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                                                                      |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                                                         |
| Acceptance criteria | In namespace `client-ns`: pod `client-pod` (label: `role=client`), pod `backend-pod` (label: `role=backend`, port `8080`), pod `external-pod` (label: `role=external`, port `8080`).<br/>Create a NetworkPolicy for `client-pod` that:<br/>- Applies `policyTypes: [Egress]`<br/>- Allows egress only to pods with label `role=backend` on port `8080`<br/>- Blocks all other egress from `client-pod` |
---
|       **19**        | **Fix a broken NetworkPolicy**                                                                                                                                                                                                                                                                                               |
| :-----------------: | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                                                           |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                                              |
| Acceptance criteria | In namespace `fix-np-ns`: pod `gateway-pod` (label: `role=gateway`) and pod `api-service` (label: `role=api`, port `8080`).<br/>NetworkPolicy `allow-gateway` exists but has a bug: the `from` podSelector incorrectly uses `role=api` instead of `role=gateway`.<br/>Fix the NetworkPolicy so that `gateway-pod` can reach `api-service` on port `8080`. |
---
|       **20**        | **Create an Ingress with TLS termination**                                                                                                                                                                                                          |
| :-----------------: | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                  |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                     |
| Acceptance criteria | In namespace `tls-ns`: Deployment `secure-app` and Service `secure-svc` (port=`80`) already exist. TLS cert and key files are at `/home/ubuntu/tls/tls.crt` and `/home/ubuntu/tls/tls.key`.<br/>- Create TLS Secret `tls-secret` in `tls-ns` from the cert and key files<br/>- Create Ingress `secure-ingress` with: host=`secure.example.com`, TLS using `tls-secret`, path=`/` (Prefix) → `secure-svc:80` |
---
|       **21**        | **Create an Ingress with path rewriting**                                                                                                                                                                                                                                                                                                                            |
| :-----------------: | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                                                                                                   |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                                                                                      |
| Acceptance criteria | In namespace `rewrite-ns`: Deployment `api-backend` and Service `api-svc21` (port=`8080`) already exist.<br/>Create Ingress `rewrite-ingress` that:<br/>- Uses annotations: `nginx.ingress.kubernetes.io/rewrite-target: /$2` and `nginx.ingress.kubernetes.io/use-regex: "true"`<br/>- Route path `/api(/\|$)(.*)` (Prefix) to `api-svc21:8080`<br/>This strips the `/api` prefix when forwarding requests to the backend. |
---
|       **22**        | **Create an Ingress with multiple virtual hosts**                                                                                                                                                              |
| :-----------------: | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                             |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                |
| Acceptance criteria | In namespace `vhost-ns`: Services `blog-svc` (port=`80`) and `shop-svc` (port=`80`) already exist.<br/>Create a single Ingress `vhost-ingress` with two host-based routing rules:<br/>- `blog.example.com` → `blog-svc:80` (path `/`, Prefix)<br/>- `shop.example.com` → `shop-svc:80` (path `/`, Prefix) |
---
|       **23**        | **Fix an Ingress with wrong ingressClassName**                                                                                                                                                                                                                                    |
| :-----------------: | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                                                                 |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                    |
| Acceptance criteria | Ingress `web-fix-ingress` in namespace `ingfix-ns` is not routing traffic because `ingressClassName` is set to `traefik`, but the available IngressClass is `nginx`.<br/>Fix the Ingress by changing `ingressClassName` to `nginx`. Verify the backend `web-fix-svc:80` is unchanged. |
---
|       **24**        | **Create an ExternalName Service**                                                                                                                                                                                                       |
| :-----------------: | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 4%                                                                                                                                                                                                                                       |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                          |
| Acceptance criteria | In namespace `extname-ns`, create a Service `db-external` that:<br/>- Type: `ExternalName`<br/>- `externalName: database.example.internal`<br/>- Port: `5432`<br/>This allows pods in the cluster to access the external database using the stable DNS name `db-external.extname-ns.svc.cluster.local`. |
---
|       **25**        | **End-to-end: Service + Ingress + NetworkPolicy**                                                                                                                                                                                                                                                                                                                                                          |
| :-----------------: | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|     Task weight     | 6%                                                                                                                                                                                                                                                                                                                                                                                                         |
|       Cluster       | cluster1 (`kubectl config use-context cluster1-admin@cluster1`)                                                                                                                                                                                                                                                                                                                                            |
| Acceptance criteria | In namespace `e2e-ns`: Deployment `full-app` (label: `app=full-app`, serving port `8080`) already exists.<br/>Complete the following:<br/>1. Create Service `full-svc`: type=`ClusterIP`, port=`80`, targetPort=`8080`, selector=`app=full-app`<br/>2. Create Ingress `full-ingress`: host=`full.example.com`, path=`/` (Prefix) → `full-svc:80`<br/>3. Create NetworkPolicy in `e2e-ns` allowing ingress to `full-app` pods only from the `ingress-nginx` namespace (use `namespaceSelector` with `kubernetes.io/metadata.name: ingress-nginx`) |
---
