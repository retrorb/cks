```
kubectl config use-context cluster1-admin@cluster1
```
```
# Check existing pods
kubectl get pods -n deny-all-ns --show-labels
kubectl get pods -n ext-ns
```
```
# vim deny-all.yaml

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all-ingress
  namespace: deny-all-ns
spec:
  podSelector: {}
  policyTypes:
  - Ingress
```
```
kubectl apply -f deny-all.yaml
```
```
# Verify - tester-pod should NOT reach target-pod
target_ip=$(kubectl get pod target-pod -n deny-all-ns -o jsonpath='{.status.podIP}')
kubectl exec tester-pod -n ext-ns -- wget -q --timeout=5 -O- "http://$target_ip:8080"
# should fail (exit code != 0)
```
