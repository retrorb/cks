```
kubectl config use-context cluster1-admin@cluster1
```
```
mkdir -p /home/ubuntu/kust3

# vim /home/ubuntu/kust3/kustomization.yaml

namespace: kgen-ns
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
- name: app-config
  literals:
  - APP_MODE=production
  - LOG_LEVEL=info
```
```
# Apply
kubectl apply -k /home/ubuntu/kust3

# Verify
kubectl get configmap app-config -n kgen-ns
kubectl get configmap app-config -n kgen-ns -o yaml
```
