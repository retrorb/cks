```
kubectl config use-context cluster1-admin@cluster1
```
```
# vim egress-dns.yaml

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-https-dns-egress
  namespace: egress-ns
spec:
  podSelector:
    matchLabels:
      app: worker
  policyTypes:
  - Egress
  egress:
  - ports:
    - protocol: TCP
      port: 443
  - ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
```
```
kubectl apply -f egress-dns.yaml
```
```
# Verify policy structure
kubectl get networkpolicy -n egress-ns -o yaml
```
