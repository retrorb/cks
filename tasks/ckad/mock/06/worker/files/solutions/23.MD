```
kubectl config use-context cluster1-admin@cluster1
```
```
# Check the broken ingress
kubectl get ingress web-fix-ingress -n ingfix-ns -o yaml
# Note: ingressClassName is set to "traefik" but should be "nginx"
```
```
# Fix option 1: patch
kubectl patch ingress web-fix-ingress -n ingfix-ns --type=json \
  -p='[{"op":"replace","path":"/spec/ingressClassName","value":"nginx"}]'
```
```
# Fix option 2: edit
# kubectl edit ingress web-fix-ingress -n ingfix-ns
# Change: ingressClassName: traefik  ->  ingressClassName: nginx
```
```
# Verify
kubectl get ingress web-fix-ingress -n ingfix-ns -o jsonpath='{.spec.ingressClassName}'
kubectl get endpoints web-fix-svc -n ingfix-ns
```
