```
kubectl config use-context cluster1-admin@cluster1
```
```
# Check failing release
helm list -n helm-broken-ns
kubectl get pods -n helm-broken-ns
```
```
# Uninstall the broken release
helm uninstall broken-release --namespace helm-broken-ns
```
```
# Install fresh release
helm install fixed-release bitnami/nginx \
  --namespace helm-broken-ns \
  --set replicaCount=2
```
```
# Verify
helm list -n helm-broken-ns
kubectl get pods -n helm-broken-ns
```
